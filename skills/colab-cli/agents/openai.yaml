interface:
  display_name: "Colab CLI"
  short_description: "Manage Google Colab sessions and run code on remote VMs with real colab CLI syntax"
  default_prompt: "Use the colab-cli skill. Prefer real commands only (new/sessions/exec/run/stop). Raise --timeout above 30s for long jobs, always name sessions with -s, and stop sessions when done."
